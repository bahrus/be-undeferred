# be-undeferred [TODO]

```html
<template
    be-undeferred='{
        "on": "my-element",
        "whereMediaMatches": "..."
    }'
    defer-be-switched
    be-switched="on when..."
>
</template>
```

